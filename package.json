{
  "name": "cengebulmaca",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "gen": "node tools/generate.mjs",
    "icons": "node tools/generate-icons.mjs",
    "test": "vitest run",
    "android": "npm run build && cap sync android && cd android && gradlew.bat assembleDebug",
    "android:release": "npm run build && cap sync android && cd android && gradlew.bat assembleRelease"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "jsdom": "^29.1.1",
    "sharp": "^0.35.3",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@fontsource-variable/nunito": "^5.2.7",
    "@revenuecat/purchases-capacitor": "^13.2.3",
    "firebase": "^12.16.0"
  }
}
